const scores = [9, 8, 7, 7, 5, 4, 9, 0];

//  join produces the comma-separated string representation of the elements
const st = scores.join();

//  concat produces a new concatenated array
const na = scores.concat([99, 97, 76]);
